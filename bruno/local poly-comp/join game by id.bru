meta {
  name: join game by id
  type: http
  seq: 13
}

post {
  url: http://localhost:3000/games/{{GAME_ID}}/join
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
